# Root conftest.py — configures pytest-asyncio for the entire test suite.
# asyncio_mode is set to "auto" in pyproject.toml so async tests
# are collected without needing @pytest.mark.asyncio on each one.
